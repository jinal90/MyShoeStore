<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <!--  Usage: LinearLayout just as a container for cardview.  -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:paddingTop="5dp"
        android:paddingBottom="5dp">

        <androidx.cardview.widget.CardView
            android:id="@+id/card_view"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_gravity="center"
            app:cardBackgroundColor="@color/cardBackgroundTransparent"
            app:cardCornerRadius="4dp">

            <!--  Usage: LinearLayout to stack all the textviews
            vertically in the row layout. -->
            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:padding="8dp"
                android:orientation="vertical">

                <TextView
                    android:id="@+id/shoeNameTextView"
                    style="@style/TextLabelCard"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    tools:text="Shoe Name" />

                <TextView
                    android:id="@+id/shoeSizeTextView"
                    style="@style/TextLabelCard"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    tools:text="Shoe Size" />

                <TextView
                    android:id="@+id/shoeCompanyTextView"
                    style="@style/TextLabelCard"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    tools:text="Shoe Company" />

                <TextView
                    android:id="@+id/shoeDescriptionTextView"
                    style="@style/TextLabelCard"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    tools:text="Shoe Description" />

            </LinearLayout>
        </androidx.cardview.widget.CardView>


    </LinearLayout>
</layout>
